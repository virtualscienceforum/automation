{
  "private": true,
  "name": "vsf-worker",
  "version": "1.0.0",
  "description": "The main cloudflare worker for VSF",
  "main": "cloudflare/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Evert van Nieuwenburg <evert.v.nieuwenburg@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
